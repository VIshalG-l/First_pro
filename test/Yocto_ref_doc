Yocto imx-5.15.71-2.2.2.xml  (NXP i.MX) Setup  Fix Guide


🔹 1. Install Required Packages

sudo apt update
sudo apt install gawk wget git diffstat unzip texinfo gcc build-essential \
chrpath socat cpio python3 python3-pip python3-pexpect xz-utils \
debianutils iputils-ping python3-git python3-jinja2 libegl1-mesa \
libsdl1.2-dev pylint xterm liblz4-tool zstd

🔹 2. Install repo Tool


mkdir -p ~/bin
curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
chmod a+x ~/bin/repo
echo 'export PATH=~/bin:$PATH' >> ~/.bashrc
source ~/.bashrc

Verify:

repo --version

🔹 3. Download Yocto BSP (imx-5.15.71-2.2.2.xml)


mkdir ~/Yocto_Project
cd ~/Yocto_Project

repo init -u https://github.com/nxp-imx/imx-manifest \
-b imx-linux-kirkstone \
-m imx-5.15.71-2.2.2.xml

🔹 4. Sync Source
repo sync -j$(nproc)

🔹 5. Setup Build

MACHINE=imx8mpevk DISTRO=fsl-imx-xwayland source imx-setup-release.sh -b build-xwayland

Accept license:
y

download liblz4-tool

sudo apt install liblz4-tool

 == build == 
 
 run cmd : bitbake imx-image-full 
 

